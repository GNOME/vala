void main () {
	{
		int if;
		if = 1;
		assert (if == 1);
	}
	{
		int switch;
		switch = 2;
		assert (switch == 2);
	}
	{
		int while;
		while = 3;
		assert (while == 3);
	}
	{
		int do;
		do = 4;
		assert (do == 4);
	}
	{
		int for;
		for = 5;
		assert (for == 5);
	}
	{
		int foreach;
		foreach = 6;
		assert (foreach == 6);
	}
	{
		//FIXME
		//int break;
		//break = 7;
		//assert (break == 7);
	}
	{
		//FIXME
		//int continue;
		//continue = 8;
		//assert (continue == 8);
	}
	{
		int return;
		return = 9;
		assert (return == 9);
	}
	{
		//FIXME
		//int yield;
		//yield = 10;
		//assert (yield == 10);
	}
	{
		int throw;
		throw = 11;
		assert (throw == 11);
	}
	{
		int try;
		try = 12;
		assert (try == 12);
	}
	{
		int lock;
		lock = 13;
		assert (lock == 13);
	}
	{
		int unlock;
		unlock = 14;
		assert (unlock == 14);
	}
	{
		int delete;
		delete = 15;
		assert (delete == 15);
	}
	{
		int with;
		with = 16;
		assert (with == 16);
	}
}
