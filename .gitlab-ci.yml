image: fedora:rawhide

before_script:
    - dnf install -y bison dbus-x11 flex glib2-devel gobject-introspection-devel graphviz-devel libxslt vala

stages:
  - build

fedora-x86_64:
  stage: build
  script:
    - ./autogen.sh --enable-debug --enable-coverage --disable-dependency-tracking
    - make
    - make bootstrap
    - make check
  except:
    - tags
